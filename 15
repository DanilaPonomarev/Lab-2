#include <stdio.h>
int main()
{
    int m, n, a=6;
    a=6*1024;
    scanf("%d %d", &m, &n);
    if((m*60+n)*16<=a)
        printf("Да, поместяться");
    else
        printf("Нет, не поместяться");
}
